function Map(maps, doors, items) {
    var map = {
        maps: maps,
        doors: doors,
        items: items,
        current: 0 // the current map (floor) to show.
    };
    
    this.drawMap = function(element) {
        
    }
    
    this.updateCharacters = function(characters) {
        
    }
}